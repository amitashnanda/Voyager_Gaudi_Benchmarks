# Requirements for PMPQ Quantization scripts (Gaudi HPU)
#
# NOTE: Do NOT install torch, habana_frameworks, or optimum-habana here.
#   - torch + habana_frameworks come from the Gaudi Docker image
#   - optimum[habana] is installed system-wide via:
#       pip install --upgrade-strategy eager optimum[habana]
#     (this pulls the correct transformers, optimum, accelerate versions)
#
# This file only lists PROJECT dependencies installed to Ceph via --target.


datasets>=2.19.0
scikit-learn>=1.4.0
tqdm>=4.66.0
numpy>=1.26.0
scipy>=1.12.0
